# 聚合计算 · 代码审查与优化建议

> **审查日期**: 2026-08-17 | **基线**: 全量回归 233/233 ✅  
> **审查范围**: 主包 24 JS 文件（5264 行）+ 其余源文件，总计 ~7109 行

---

## 审查结论

整体代码质量健康：架构清晰（注册表驱动 + 通用 Behavior + Canvas 2.0 出图）、测试覆盖完整（233 条）、无安全性问题。以下优化建议按"收益/风险"分级。

---

## P0 — 必须处理（无）

未发现编译错误、数据竞争、用户数据丢失或安全漏洞。

---

## P1 — 推荐优化（收益明确，风险可控）

### 1️⃣ relation.js LOOKUP 表：删除 56 个重复条目（约 1.2KB 冗余）

**问题**：`LOOKUP` 表中 18 组键重复 4 次（如 `'父,子,子'`、`'兄,妻,子'` 等），另有 `'姑妈'`、`'姨妈'` 重复 2 次。这些是历史多次手工补充时的遗留，归约引擎只匹配首次命中，后续重复条目永不被访问。

**量化**：
```
278 个唯一键中有 56 个重复
18 组 ×4 + 2 组 ×2 = 56 个多余条目
浪费 ~1,239 字符，占文件 ~5.5%
```

**风险**：低。只删重复，不改逻辑。需保留 1 份，删其余 3 份。

**建议**：删除重复条目，保留每组第一次出现的那一个。测试需确认 233 条回归不变。

---

### 2️⃣ report.js：提取共用 `roundRect` 函数（消除 3 份重复圆角矩形代码）

**问题**：`drawBrandStrip`（品牌卡 + 码框）和 `drawBadge` 各自独立实现了 3 遍 `arc()/closePath()/fill()` 圆角矩形，每份 ~16-17 行，代码高度相似。

**量化**：
```
3 份圆角矩形实现，合计 ~50 行重复样板
```

**建议**：抽离为：
```javascript
function drawRoundRect(ctx, x, y, w, h, r, fillStyle) {
  ctx.beginPath()
  ctx.moveTo(x + r, y)
  ctx.lineTo(x + w - r, y)
  ctx.arc(x + w - r, y + r, r, -Math.PI / 2, 0)
  ctx.lineTo(x + w, y + h - r)
  ctx.arc(x + w - r, y + h - r, r, 0, Math.PI / 2)
  ctx.lineTo(x + r, y + h)
  ctx.arc(x + r, y + h - r, r, Math.PI / 2, Math.PI)
  ctx.lineTo(x, y + r)
  ctx.arc(x + r, y + r, r, Math.PI, -Math.PI / 2)
  ctx.closePath()
  ctx.fill()
}
```
3 处调用点改为 `drawRoundRect(ctx, x, y, w, h, r, fillStyle)`，无功能变化。

---

### 3️⃣ calc-page.js：拆分 `saveImage` 回调嵌套（深度 9 层 → 4 层）

**问题**：`saveImage` 函数最大嵌套深度 9 层（`query.exec → if → canvas → try → if → afterDraw → if/else → Promise.then/catch → try`），可读性偏低。

**建议**：将 `afterDraw`、`handleDraw`、`exportWithQr` 等独立逻辑提取为命名函数。当前功能正确，但修改此处的风险中等（涉及 Canvas 2.0 异步流程），可视后续改动时一并重构。

---

## P2 — 可考虑优化（收益有限或风险略高）

### 4️⃣ app.js `showInterstitial`：`ad.onClose(done)` 去重注册

**问题**：每次调用 `showInterstitial` 都会 `ad.onClose(done)`，但 `done` 内部又 `ad.offClose(done)`。如果连续快速调用，可能叠加多个 onClose 回调。

**影响**：当前插屏广告 5 分钟冷却已遏制频繁调用；仅作为代码整洁建议。

**建议**：在 `showInterstitial` 入口先 `ad.offClose(done)` 再 `ad.onClose(done)`，确保唯一侦听。

### 5️⃣ wealth-health.js `reset()` 优化：用已知默认对象替代逐字段赋值

**问题**：`reset()` 方法约 18 行逐一重置字段，可读性低且与初始 `data` 存在同步风险（新增字段容易漏 reset）。

**建议**：维护一份 `DEFAULT_DATA` 对象，`reset()` 直接 `this.setData(defaults)`。示例做法见 `tests.js` 的 `sharedDefaults` 模式。

### 6️⃣ history.js `star()`：可用 `findIndex` + 原地修改

**问题**：
```javascript
function star(id) {
  var list = all()
  // 当前：手动循环 → break → save
  for (var i = 0; i < list.length; i++) {
    if (list[i].id === id) { list[i].star = !list[i].star; break }
  }
  save(list)
}
```
当前写法无功能问题，但用 `findIndex` 更清晰。

---

## P3 — 可做可不做（代码风格/微小优化）

### 7️⃣ 测试文件变量名一致性

`runAllTests.js` / `test_all.js` 中部分函数使用了 `saveAlbumCalls`（第 23 行定义 `saveAlbumCalls`），但 `assertSaveOK` 内部使用了 `saveAlbumCalls`（带 "e"）—— **已统一为 `saveAlbumCalls`**，实际通过测试证明一致，无需修改。仅作观察记录。

### 8️⃣ `format.js` 中 `formatMoney` 的 Number -> String 迂回路

当前：
```javascript
var parts = String(num).split('.')
```
当 `num` 很大时（如 `1234567890.12`），`String(num)` 可能会输出科学计数法（`1.23456789012e+9`），导致 `split('.')` 结果异常。但实际场景中金额不会达到这个量级（用户输入在万元级），风险极低。

**建议**：如需严谨，可换 `num.toLocaleString('en-US')` 或 `Intl.NumberFormat`。

### 9️⃣ `config/tools.js` 工具顺序：建议按使用频率排序

当前顺序：塔罗 → 财富层级 → 财富健康 → 亲戚关系 → 家庭支出 → BMI → 时光进度。当前方案合理（热点在前），无需修改，如有数据可按首页点击埋点调整。

---

## 总结

| 级别 | 数量 | 关键项 |
|------|------|--------|
| P0 | 0 | 无严重问题 |
| **P1** | **3** | LOOKUP 重复(56条)、roundRect 抽离、saveImage 嵌套 |
| P2 | 3 | onClose 去重、reset 默认对象、history findIndex |
| P3 | 3 | 测试命名、formatMoney 科学计数法、工具排序 |
| **合计** | **9** | — |

所有优化均为**非破坏性**：不改数据模型、不改API签名、不改测试断言。修改后 233 条回归预期全绿。

> **建议优先实施**：P1 #1（relation.js 去重）与 #2（roundRect 抽离）—— 收益直观且风险最低。