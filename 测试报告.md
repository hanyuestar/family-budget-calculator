# 测试报告

> 项目：聚合计算（family-budget-calculator）
> 最后测试：2026-07-29（财富健康指数新增、存钱段位删除后的全量回归）
> 说明：本文件为唯一测试记录，后续所有测试结果均追加于此。

---

## 一、计算逻辑测试（2026-07-14 早）

**方法**：逐字复制三个页面的纯计算逻辑成 Node 测试脚本，含真实 `utils/format.js`，实跑边界值、负数、极值。

| 模块 | 用例数 | 结果 |
|------|--------|------|
| formatMoney / formatWan | 11 | ✅ 全通过 |
| 支出计算 | 5 | ✅ 全通过 |
| 财富等级匹配 | 10 | ✅ 全通过 |
| 财富等级扫描（0→2000，步进0.5，无重复/无遗漏） | 1 | ✅ 通过 |
| 存钱段位匹配 | 7 | ✅ 全通过 |
| 财富年龄插值 | 6 | ✅ 全通过 |
| 实物对比 | 4 | ✅ 全通过 |
| 存钱计算（正常/支出>收入/收入=0） | 3 | ✅ 全通过 |

**总计：47/47 断言通过，核心计算逻辑无 bug。**

---

## 二、重构后回归测试（2026-07-14 中）

**方法**：运行时打桩（stub wx/Page/Behavior/getApp/getCurrentPages），加载真实页面文件调用 `calculate`。

| 页面 | 用例 | 结果 |
|------|------|------|
| 支出计算（index） | 月度/年度合计、保险占比 | ✅ 全通过 |
| 财富测试（wealth） | 7级等级 + 负数回退 | ✅ 全通过 |
| 存钱段位（saving） | 段位/财富年龄/实物对比/分享写入 | ✅ 全通过 |

**总计：17/17 断言通过，重构后计算逻辑与原版一致。**

---

## 三、首页跳转修复测试（2026-07-14 晚）

**问题**：首页所有工具卡片点击无反应。根因：`config.path` vs `home.data-url` 字段名不一致。

**测试**：加载 `home.js` 模拟点击 6 个工具，断言 `wx.navigateTo` 参数与 `config.tools[].path` 一一对应。

**结果：10/10 断言通过，全站点击恢复。**

---

## 四、新增工具集成测试（2026-07-14 晚）

### 4.1 BMI 计算器
| 用例 | 结果 |
|------|------|
| 标准身高体重 → BMI值 | ✅ |
| 偏瘦边界（BMI=18.4） | ✅ |
| 超重边界（BMI=24.0） | ✅ |
| 零输入 → 不显示结果 | ✅ |

### 4.2 进度条（时光进度条）
| 用例 | 结果 |
|------|------|
| 今年/本月/今日进度 % | ✅ |
| 出生日期 → 年龄 + 人生进度 | ✅ |
| 未来日期 → toast 报错 | ✅ |
| 空日期 → 隐藏人生进度 | ✅ |

### 4.3 亲戚关系（归约引擎重构）
| 用例 | 结果 |
|------|------|
| 56 条测试（含超长链、绕路、姻亲、平辈归约） | ✅ 全通过 |
| 配偶称呼（妻子/丈夫非"配偶"） | ✅ |
| 旁系堂表侄甥 + 年龄选择 | ✅ |

**总计：55/55 断言通过（test_relation.js 实跑）。**

---

## 五、亲戚关系保留验证（2026-07-14 末）

**方法**：自动化脚本扫描项目，检查文件存在性、路由一致性。

| 检查项 | 结果 |
|--------|------|
| app.json 含 relation 路由 | ✅ |
| config/tools.js 含 relation 条目 | ✅ |
| pages/relation/ 四文件存在 | ✅ |
| test_relation.js 存在 | ✅ |
| 6 个工具卡片数量正确 | ✅ |
| 6 个工具 path → app.json 路由一致 | ✅ |
| 6 个工具 × 4 文件（js/wxml/wxss/json）全部存在 | ✅ |
| progress 改名 "时光进度条"（config + json） | ✅ |
| home.js goTool 读 `dataset.path` | ✅ |
| home.wxml 绑 `data-path="{{item.path}}"` | ✅ |
| app.json 共 7 页面（home + 6 工具） | ✅ |

**总计：11/11 断言通过。**

---

## 六、已知未修复问题

| 编号 | 问题 | 严重度 | 状态 |
|------|------|--------|------|
| P1-1 | saving 负支出未校验：输入负支出 → 储蓄率 > 100% 命中"王者" | 🟡 | ✅ 已修复（2026-07-14，income/expense 非负 clamp，见第八节） |
| P1-2 | 结果区淡入动画因 `wx:if`+`class` 同时切换而失效 | 🟡 | ✅ 已修复（2026-07-14，wealth.wxss 改 transition→animation keyframes） |
| P1-3 | index/wealth 实时算 vs saving 点按钮，交互不统一 | 🟡 | 🟡 待修（产品决策：实时 vs 点按钮，非功能 bug） |
| P2-1 | 插屏 `onClose` 监听器累积泄漏 | 🟢 | ✅ 已修复（2026-07-14，done 内 offClose 自清理） |
| P2-2 | Canvas API 使用已废弃的 `createCanvasContext` | 🟢 | 🟢 待修（仅弃用警告，功能正常；全量迁移 Canvas 2D 涉及 5 页+report.js，风险高，需真机回归） |
| P2-3 | `calcCompare` 负数无保护（当前被 `Math.max(0,...)` 挡住） | 🟢 | ✅ 已处理（原本就有 Math.max(0,...) 保护） |
| P3-1 | 所有输入无上限校验 | 🟢 | ⚪ 待定 |

---

## 七、测试方法说明

后续测试按以下方式执行：

1. **计算逻辑**：Node 脚本逐字复制页面函数，用 `new Function()` 加载后传入不同输入验证。
2. **页面行为**：stub `wx`/`Page`/`Behavior`/`getApp`/`getCurrentPages`，加载真实页面 JS，调用各方法（`calculate`/`saveResult`/`goHome`/`onShareAppMessage` 等）。
3. **静态检查**：脚本扫描项目文件，验证文件存在性、路由一致性。
4. **语法检查**：`node --check` 全部 .js 文件。

---

## 八、本轮修复与回归（2026-07-14）

> 目标：以最新文档为准——保留亲戚关系（归约引擎已重构）、保留时光进度条，并修复已知 bug。

### 8.1 亲戚关系归约引擎重构（保留并强化）
- 归约引擎扩至 6 条规则：姻亲归约/配偶子女归约/兄弟姐妹父母归约/平辈孩子归约/平辈互消/双层绕路
- 调整执行顺序为先归约后长度检查，支持超长链（6 级"兄→弟→姐→妹→女→子"归约为 3 级查表）
- 修复 L1 无限递归 bug 和平辈归约缺失 bug
- 5×2 卡片式布局重构
- `test_relation.js` 55/55 全通过

### 8.2 bug 修复与验证
| 编号 | 修复点 | 验证方式 | 结果 |
|------|--------|----------|------|
| P1-1 | `saving.js` income/expense 加 `Math.max(0, …)` 非负 clamp | 桩加载真实 saving.js，跑 7 用例 | ✅ 7/7（负支出不再 >100%；费用>收入→0%；负收入→0%） |
| P1-2 | `wealth.wxss` 改 `transition`→`@keyframes fadeInResult` + `animation … forwards` | 代码核对（wx:if 插入即带 `.show`，animation 自动播放） | ✅ |
| P2-1 | `app.js` `done` 内 `ad.offClose(done)` 自清理，catch 同样 offClose | 代码核对（每次展示至多 1 个监听） | ✅ |
| P2-3 | 原已 `Math.max(0,…)` 保护 | 代码核对 | ✅ 无需改 |

### 8.3 语法与残留
- `node --check` 全部 .js 文件：✅ 通过
- relation 相关文件全部存在且正常

### 8.4 本轮未修（说明）
- **P2-2**：`createCanvasContext` 已弃用，但功能正常；全量迁移 Canvas 2D 涉及 index 饼图 + report.js + 7 页 saveResult，改动大、需真机回归，暂缓。
- **P1-3**：实时 vs 点按钮交互差异属产品决策，非缺陷，暂缓。
- **P3-1**：输入上限校验待定。

---

## 九、改动后全功能回归（2026-07-14 末）

**背景**：修复已知 bug 后，需确认改动未破坏其余功能。

**方法**：与第七节一致——运行时打桩（`wx`/`Page`/`Behavior`/`getApp`/`getCurrentPages`），加载**真实页面文件**调用 `calculate`/`onLoad`/`onInputBirth`/`goTool` 等方法；用 `structuredClone` 深拷贝 `data`。覆盖 6 个工具 + 首页跳转。

| 模块 | 用例 | 结果 |
|------|------|------|
| format.js（千分位/1位小数/负数/NaN） | 9 | ✅ 全通过 |
| 家庭支出 index（月/年合计、保险占比三色、饼图过滤0值、复杂场景） | 10 | ✅ 全通过 |
| 财富层级 wealth（7级边界、负数回退、负债率、reset） | 15 | ✅ 全通过 |
| 存钱段位 saving（5段位边界、负支出/负收入 clamp、财富年龄插值、实物对比、零输入） | 14 | ✅ 全通过 |
| BMI（4分类+边界、健康区间、零输入隐藏） | 10 | ✅ 全通过 |
| 时光进度条 progress（年/月/日进度、人生进度公式一致、未来日期 toast、空日期隐藏） | 9 | ✅ 全通过 |
| 首页 home（工具数=6、gridCols 自适应、6卡片 navigateTo 与 config.path 一致、分享标题） | 4 | ✅ 全通过 |

**亲戚关系**：归约引擎 55/55 测试通过（`test_relation.js` 实跑），覆盖 L1~L5、堂表年龄选择、姻亲/平辈/绕路归约、超长链归约、边界拦截。

**结论**：全部功能正常，改动未引入回归。

---

## 十、测试桩同步与三套测试全绿（2026-07-15）

**背景**：用户重构后代码（饼图迁移 Canvas 2.0、saveImage 公共脚手架上提 calc-page、onInput 去重），需同步测试桩期望值并全量回归。

### 10.1 测试桩同步项（`fbc_audit/run_tests.js` 第 6 节）
| 断言项 | 旧期望 | 新期望 | 原因 |
|--------|--------|--------|------|
| `wx.createSelectorQuery` 全项目调用数 | 1 | **2** | calc-page 公共脚手架 + index 饼图 Canvas 2.0 均使用 |
| `wx.createCanvasContext` 全项目调用数 | 2 | **0** | index 饼图已完成 Canvas 2.0 迁移（S1 技术债已清） |

### 10.2 三套测试全绿结果

| 测试套件 | 结果 | 关键指标 |
|---------|------|----------|
| **全量回归** `run_tests.js` | **135 PASS / 0 FAIL** | 计算逻辑 6工具、6页 saveResult、历史端到端、语法/JSON/静态检查、relation 55/55、重构回归 9 条 |
| **压力/稳定性** `stress_test.js` | **0 失败** | A 高频计算 5000×（≈6 万 ops/s）、A2 输入风暴 3000×、B 连续保存 100×（全链路触发 100/100/100/100，内存 Δ0.14MB）、C 历史混合 3000×、D 混合极端 500×（保存累计 267 次） |
| **健壮性/失败路径** `robustness_test.js` | **0 失败** | R1 节点判空、R2 no-permission 引导授权、R3 auth-deny 引导、R4 生成失败兜底——均绿 |

### 10.3 关键验证点
- **R1/R2 重构去重落地**：`wx.createSelectorQuery` 仅 2 处（calc-page + index），`onInput:function` 仅 3 处（calc-page 共享 / calc-input 组件 / saving 自定义 doCalculate），index/wealth/bmi 已无私有 `onInput`。
- **Canvas 1.0 彻底清零**：`wx.createCanvasContext` 0 处，S1 技术债已清。
- **保存链路健壮**：`saveImageToPhotosAlbum` fail 正则覆盖 `auth|deny|permission|cancel|not\s*allow|no\s*perm`，`calc-page.js` 节点判空到位，真机体验版已验证授权弹窗→存图成功、系统拒绝相册后再保存弹"去设置"引导。

### 10.4 结论
三套测试**零失败、零回归**，功能层面已可直接发布。仅剩非阻塞项：P3-1 输入上限校验（待定）、P1-3 实时 vs 点按钮交互差异（产品决策）。

---
