# 聚合计算 · 塔罗运势功能方案（PRD v0.1 草案）

> 状态：方案评审中（未动工）  ·  日期：2026-07-17  ·  受众：90 后 / 00 后
> 定位：**娱乐测试 / 心理投射**，非算命预测（合规红线见 §8）

---

## 1. 功能概述与目标

| 项 | 内容 |
|---|---|
| 目标 | 在「聚合计算」小程序内新增一个轻量、高传播的**塔罗占卜**工具，作为流量与分享增益点 |
| 选塔罗而非六爻的原因 | 受众认知度高（"抽卡"文化契合）、视觉强、易出分享图、纯前端随机即可实现，与现有架构同构；六爻门槛高、视觉弱、分享欲低（详见评审结论） |
| 核心闭环 | 选牌阵 → 点击抽牌（洗牌动画）→ 看结果 → 一键出分享图 / 转发 → 扫码回流 |
| 复用基建 | `config/tools.js` 注册表、`behaviors/calc-page.js`（出图/分享/返回）、`utils/report.js`（Canvas 出图+品牌条）、`utils/history.js`（本地历史） |

---

## 2. 用户路径

```
首页卡片「塔罗运势」
   └─ 选择牌阵（每日一牌 / 三张时间流 / 关系决策阵）
        └─ （可选）输入一个问题/心念（calc-input 组件，影不影响算法，仅作氛围）
             └─ 点击「开始抽牌」→ 洗牌+逐张翻牌动画
                  └─ 结果页：牌面 + 正/逆位 + 关键词 + 解读 + 底部免责声明
                       ├─ 保存到相册（分享图，带品牌条+小程序码）
                       ├─ 转发给好友（onShareAppMessage）
                       └─ 存入本地历史（history.js，可回看）
```

---

## 3. 牌阵设计（首版 3 个，互斥可选）

| 牌阵 | 抽牌数 | 位置含义 | 适用场景 |
|---|---|---|---|
| **每日一牌** | 1 张 | 今日指引 | 高频、低门槛，适合每天来一次，培养日活 |
| **三张时间流** | 3 张 | 过去 / 现在 / 未来 | 通用运势、阶段复盘 |
| **关系决策阵** | 4 张 | 现状 / 阻碍 / 建议 / 可能结果 | 感情、选择、职场决策（高互动） |

> 后续可扩展：恋爱心态阵（5 张）、年运阵（12 张）等。架构上牌阵=配置项，加阵≈加一条数据。

---

## 4. 牌库与文案数据结构

78 张牌（大阿尔卡纳 22 + 小阿尔卡纳 56）。**文案是核心资产，建议分批产出**（首版每牌「3 关键词 + 1 段 60–120 字解读」，逆位单独一套）。

数据文件：`miniprogram/data/tarot-cards.js`（纯前端，随包发布，零接口成本）

```js
// miniprogram/data/tarot-cards.js
module.exports = [
  {
    id: 0,
    arcana: 'major',          // major | minor
    number: 0,
    name: '愚者',
    en: 'The Fool',
    suit: null,               // minor 时为 cups/wands/swords/pentacles
    element: '风',
    upright: {
      keywords: ['新的开始', '自由', '勇气'],
      text: '一张白纸的旅程即将展开。今天适合放下顾虑，大胆踏出第一步……'
    },
    reversed: {
      keywords: ['冲动', '犹豫', '风险'],
      text: '逆位的愚者提醒你：出发前先看清脚下的路，别让莽撞变成隐患……'
    }
  },
  // …… 其余 77 张
]
```

正/逆位判定：抽牌时对每张独立随机（约 50% 概率逆位）。展示与文案均走对应分支。

---

## 5. 随机抽取算法（纯前端，无后端）

```js
function drawCards(deck, count) {
  const pool = deck.slice()
  const result = []
  for (let i = 0; i < count; i++) {
    const idx = Math.floor(Math.random() * pool.length)
    const card = pool.splice(idx, 1)[0]
    result.push({
      card,
      reversed: Math.random() < 0.5   // 正/逆位
    })
  }
  return result   // 顺序即牌阵位置顺序
}
```

- 无重复抽牌（`splice` 保证同一局不重复）。
- 不依赖 `wx.getRandomValues` 之外的任何能力，真机表现稳定。

---

## 6. 分享图出图方案（复用 `report.js`）

塔罗页 `saveImage` 调用与现有计算器**同构**，只需提供一个 `draw(ctx, W, H, data)` 函数绘制牌面区，其余 header / 品牌条 / 导出由 `calc-page.js` 自动完成。

```js
// pages/tarot/tarot.js —— 出图主体（伪代码）
this.saveImage({
  title: '我的塔罗运势',
  theme: ['#6a5acd', '#8a2be2'],     // 塔罗紫调，区别于其它工具
  slogan: '今日指引 · 仅供娱乐',
  hook: '扫码抽你的今日塔罗',
  footer: '本结果为娱乐测试，不构成任何建议',
  H: 610,
  draw: function (ctx, W, H, data) {
    let y = 110
    data.drawn.forEach(function (d, i) {
      // 画牌框（逆位加角标「逆」而非旋转文字，保证可读）
      drawCardBox(ctx, 30, y, W - 60, 96, d.card, d.reversed)
      y += 110
    })
  }
})
```

**牌面视觉决策（待确认）**：
- **方案 A（MVP 推荐）**：Canvas 矢量绘制极简卡面 = 牌名 + 关键词 + 一句金句，**零图片资源、零版权成本、可立即上线**。
- **方案 B（迭代）**：国潮/水墨风格插画卡面（需设计资源或外采，体验更佳、更想转发）。

> 建议先 A 跑通闭环验证数据，再视分享率决定是否投入 B。

---

## 7. 接入点改造清单

| 改动 | 文件 | 内容 |
|---|---|---|
| 注册表 | `miniprogram/config/tools.js` | `tools` 数组加一项：`{ id:'tarot', icon:'🔮', name:'塔罗运势', desc:'选牌阵抽一张，今日指引一键出图', path:'/pages/tarot/tarot', hot:true }` |
| 页面路由 | `miniprogram/app.json` | `pages` 数组加 `'pages/tarot/tarot'` |
| 页面四件套 | `miniprogram/pages/tarot/` | `tarot.{js,wxml,wxss,json}`：选阵 → 抽牌动画 → 结果 → 出图 |
| 行为复用 | `behaviors/calc-page.js` | 直接 `require`，用 `saveImage` / `goHome` / `onShareAppMessage`，无需改动 |
| 出图复用 | `utils/report.js` | 直接 `require`，无需改动（共享品牌条+小程序码） |
| 历史 | `utils/history.js` | 存 `{ type:'tarot', spread, cards:[{id,reversed}], ts }`，历史页可回看 |
| 数据 | `miniprogram/data/tarot-cards.js` | 新建 78 张牌文案库（最大工作量） |

> 注：除 `tarot-cards.js` 文案外，前端代码量极小——本质是把现有出图链路"换一套数据"。

---

## 8. 合规措辞清单（红线）

微信对"算命占卜"类目审核偏严，务必：
- 全站定位 **「娱乐测试 / 心理投射」**，结果页与分享图底部固定免责声明：**「本结果为娱乐测试，不构成任何建议」**。
- **禁用**词：预测未来、改运、转运、必、一定、化解、辟邪、开运等承诺/迷信措辞。
- 分享标题用体验式语气，如「我抽到【愚者】正位，今天适合大胆开始！」而非「你将转运」。
- 不接入付费解牌、不引导到外部占卜服务（避免踩"诱导交易"）。

---

## 9. 工程量与排期（粗估）

| 模块 | 工作量 | 说明 |
|---|---|---|
| 接入配置（§7 前 6 行） | 0.5 天 | 纯配置，复用基建 |
| 页面四件套 + 抽牌动画 | 1–1.5 天 | 动画可用 CSS/WXSS 翻转，简单 |
| **牌库文案（78 张 × 正逆位）** | **2–3 天** | 核心资产，建议分批，先出关键 22 张大阿尔卡纳 |
| 出图 draw 函数 + 联调真机 | 0.5 天 | 复用 report.js |
| 合计 | **约 4–5 天** | MVP（方案 A 卡面） |

---

## 10. 后续扩展（本期不做，记录在案）

1. **国潮化视觉**：水墨/敦煌/赛博国风卡面，拉近与 00 后文化距离。
2. **轻量叠加**：生肖/星座元素作为「今日幸运签」旁路，进一步增强本土亲近感。
3. **每日一牌推送**：结合订阅消息做日活唤醒（需类目资质评估）。
4. **牌阵商店**：更多阵型作为内容更新持续供给。

---

## 11. 待确认 / 待拍板

- [ ] 卡面视觉走 **方案 A（矢量极简，零成本）** 还是直接上 **方案 B（国潮插画）**？
- [ ] 首版牌阵是否就这 3 个？还是先只上「每日一牌」做最小验证？
- [ ] 文案是否需我代笔产出首版（22 张大阿尔卡纳）？
- [ ] 是否需要在结果页/分享图加「生肖/星座」旁路（§10-2）？
