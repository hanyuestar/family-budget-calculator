# 测试脚本复用指南（聚合计算）

> 用途：记录本项目全部测试/工具脚本的作用、跑法与扩展方法。
> 下次**修改/新增功能后**，按本指南直接复用现有脚本补测试，无需重新编写，节省积分与时间。
> 最后更新：2026-08-17（新增 test_buttons，全量 233 断言）

## 一、脚本一览

| 脚本 | 断言 | 作用 | 何时跑 |
|---|---|---|---|
| `miniprogram/run_tests.js` | — | **聚合入口**：依次跑 test_tarot / test_all / test_buttons，任一失败整体 exit 1 | `npm test`（改任何代码后必跑） |
| `miniprogram/test_tarot.js` | 55 | 塔罗分包全量：22 牌数据层（含 jpg 文件存在性）+ 3 牌阵 + 抽牌逻辑 + 选牌页/抽牌页页面层 + 保存出图集成层（卡面/逆位/长图不压 banner）+ 保存链路回归（各页 draw 签名 5 参、draw 抛错兜底） | 动塔罗相关代码后 |
| `miniprogram/test_all.js` | 139 | 6 个非塔罗工具全量：每工具 = 核心计算正确性 + 保存/重置/分享/历史 E2E + isSaving 防重复 + 模式切换 + 多城市社保口径 + 常识性校验 + refactor 基建回归（DPR/主题缓存、onInput 防抖、Canvas 2.0 饼图、DEDUCT_ITEMS 一致性） | 动任一工具页/utils 后 |
| `miniprogram/test_buttons.js` | 39 | **按钮/事件绑定完整性**（遍历全部 wxml 的事件绑定，核对 JS 方法存在）+ 未覆盖页面点击模拟（首页/历史页/塔罗选牌/calc-input）+ **全部 JS 严格模式解析**（防重复函数声明，微信编译器会因重名直接编译失败） | 动任何 wxml/js 后，尤其改按钮/事件绑定后 |

## 二、跑法（全部纯 Node，不依赖微信 IDE）

```bash
npm test              # 聚合三套（推荐，日常用这个）
npm run test:tarot    # 仅塔罗
npm run test:all      # 仅非塔罗工具
npm run test:buttons  # 仅按钮/绑定/严格模式
```

- Node 用 managed 版：`C:/Users/19625/.workbuddy/binaries/node/versions/22.22.2/node.exe`
  （**不能用 `.qclaw/binaries`**，不存在）。跑法：`node miniprogram/test_all.js`。

## 三、如何复用/扩展（新功能必读）

### 1. 新增一个计算工具页后（照抄现有模式）
- **逻辑测试**：在 `test_all.js` 仿照 `runBmi()` / `runWealthHealth()` 写 `run新工具()`：
  1. `fresh('pages/新工具/新工具.js')` 拿到页面实例（自动合并 calc-page behavior）；
  2. `setData({...输入})` → `calculate()` → 断言结果字段；
  3. `assertNoSave(inst, /提示文案/)` 测「未输入保存拦截」；
  4. `setData({isSaving:true})` → `saveResult()` → 断言被拦截；
  5. `await assertSaveOK(inst, 'toolId', true)` 测保存 E2E（出图+存相册+isSaving 复位+写历史）；
  6. `restoreHistory({input:{...}})` 测历史还原。
- **按钮绑定**：`test_buttons.js` 的 A 节自动遍历新页面的 wxml 绑定（无需手写）；
  如需点击模拟，在 B~F 节加一个 test 块。
- **入口接入**：新工具进 `config/tools.js` 后，`test_buttons.js` A0 严格模式解析自动覆盖；
  把 `run新工具()` 加进 `test_all.js` 主流程 `(async function(){...})()`。

### 2. 新增塔罗牌/牌阵/文案
- 改 `subpackages/tarot/data/cards.js` 后跑 `npm run test:tarot`：自动校验 22 张、idx 连续、
  img 路径存在、up/rev 三关键词、文案非空、3 牌阵 positions=count、抽牌不重复。
- 卡面图新增后必须放 `subpackages/tarot/assets/jpg/`（400×400 jpg），否则「jpg 文件真实存在」断言会失败。

### 3. 改公共基建（report.js / calc-page.js / history.js）
- 这三者是所有工具共用的，改动后**三套全跑**。
- 特别注意：`calc-page.js` 的 `saveResultTemplate(opts)` 是各页 saveResult 的统一入口，
  改它的 guard/isSaving/历史写入逻辑会影响全部工具 —— `test_buttons.js` A0 严格模式 + `test_all.js` 保存 E2E 兜底。

## 四、常见坑（写测试脚本时）

1. **严格模式重复声明**：Node 测试环境宽松，`function getDpr(){}` 写两次 Node 不报错，
   但微信编译器严格模式直接编译失败（2026-08-17 真实踩坑）。`test_buttons.js` A0 节已用
   `new Function('"use strict";'+src)` 兜底，改代码后跑 `npm run test:buttons` 即可发现。
2. **mock 结构**：三套测试共用同款 mock 骨架——`wx`（getSystemInfoSync/getStorageSync/showToast/
   canvasToTempFilePath/saveImageToPhotosAlbum/createSelectorQuery…）+ `getApp`（globalData.theme=light
   + showInterstitial）。`instantiate(opts)` 合并 behavior 的 data/methods 构造可驱动实例。
3. **setTimeout 覆盖**：test_tarot/test_buttons 顶部 `global.setTimeout = fn→fn()` 让洗牌延时等
   同步执行，便于确定性断言；`assertSaveOK` 用 `process.nextTick` + `realSetTimeout(30ms)` 等异步出图链。
4. **tarot draw 是异步 draw**（返回 Promise，`canvas.createImage` 加载分包 jpg），
   保存链路断言前要 `await` 两拍（nextTick + 30ms）。
5. **按钮绑定完整性**：wxml 里 `bind:input="onInput"`（calc-input 组件事件）与
   `bindtap`/`catchtap`/`bindchange`/`bindchanging` 都会被 test_buttons A 节提取核对；
   组件 JS 的方法在 `def.methods` 里（collectHandlers 已合并）。

## 五、改完功能后的标准动作（Checklist）

```bash
npm test                                  # 1. 全量回归（三套 233 断言）
node --check 改动过的每个.js               # 2. 语法体检（可选，A0 已含严格模式）
# 3. 真机复测（微信开发者工具 → 真机调试）：
#    - 保存图片到相册（授权后）
#    - 新增/改动页面的交互（下拉、开关、步进、饼图渲染）
git add -A && git commit -m "..."        # 4. 本地提交
# 5. 推送 GitHub：直连 + 内联 PAT（见 .workbuddy/memory/MEMORY.md 沙箱 push 约束）
```

## 六、相关文档

- `.workbuddy/memory/MEMORY.md` — 项目长期记忆（架构约定/测试约定/工具增删/Git 部署约束，权威）
- `README.md` — 对外 wiki（功能/架构/测试/发布）
- `ad_placement_guide.md` — 广告位预埋清单（上线替换真实广告 ID 时用）
- `塔罗文案_大阿尔卡纳.md` — 塔罗文案源稿（改文案/校对时参考）
- `miniprogram/subpackages/tarot/assets/jpg/` — 塔罗 22 张卡面产物（运行时依赖；源图 `tarot-sources/` 已于 2026-09-02 清理）
