# 聚合计算 · 压力与稳定性测试报告

**项目**：聚合计算（原「家庭预算计算器」微信小程序）
**测试日期**：2026-07-15
**测试对象**：当前主线代码（已含 P2–P5 + C2 修复及 R1–R3 去重重构）
**测试脚本**：`C:/Users/19625/AppData/Local/Temp/fbc_audit/stress_test.js`（与全量测试桩 `run_tests.js` 同目录）

---

## 1. 测试目的与边界

聚合计算是**纯前端、无后端**架构（计算全在端上，历史走 `wx.setStorageSync`），因此不存在「多用户打服务端」的服务端并发压测对象。本测试针对小程序在**单线程 JS 下的高频 / 连续 / 混合事件**做稳定性与耗时测量，回答三个问题：

1. 狂敲输入框 / 连点保存 / 高频读写历史时，**会不会崩、会不会算错**？
2. 连续保存链路下，**Canvas 节点与回调是否堆积、内存是否泄漏**？
3. 输入风暴 + 保存 + 历史交织的**极端组合下是否出现竞态**？

### 测试边界（重要，避免误读）

> ⚠️ 本脚本在 Node 桩中运行真实页面 JS，**桩内 `setData = Object.assign`（不走 JS↔native 桥）**，因此只测「**纯 JS 计算 + 链路调用**」的稳定性与耗时。
>
> 真实环境中的 `setData` 跨桥渲染、主线程帧率、真机堆内存与 `canvasToTempFilePath`/`saveImageToPhotosAlbum` 的原生 I/O 耗时，**无法在本脚本中体现**，须在**真机 DevTools 性能面板（Audits）**测量。本脚本是逻辑层压力验证，**不替代真机 profiling**。

---

## 2. 测试维度与结果

| 维度 | 场景 | 规模 | 总耗时 | 单次均值 | 吞吐 / 关键结论 |
|------|------|------|--------|----------|----------------|
| **A 高频计算风暴** | `index.calculate()` 全量重算（15 项输入） | 5000 次 | 82.7 ms | 0.0165 ms | **≈60,427 ops/s**；风暴后 `annualTotal=228000` 仍精确，无退化 |
| **A2 输入事件风暴** | `calc-page.onInput` 路径（模拟狂敲输入框） | 3000 次 | 30.9 ms | 0.0103 ms | 末值 `annualTotal=113175`，输入中间态处理正确 |
| **B 连续保存链路** | `index.saveResult()`（Canvas 2.0 导出 + 存相册） | 100 次 | 4.3 ms | 0.043 ms | 每轮**完整触发**整条链（见 §3）；内存 Δ=0.14 MB |
| **C 历史高频读写** | `add`/`star`/`remove`/`clear` 混合 | 3000 次 | 9.6 ms | — | 无异常、数组无越界；末态 22 条（clear 与 remove 生效） |
| **D 混合极端** | 输入风暴 + 连点保存 + 历史写入 交织 | 500 轮 | 7.1 ms | — | 无竞态、无崩溃；期间保存链路实际触发 **267 次** |

**结论**：全部维度在高频 / 连续 / 混合压力下**零崩溃、零逻辑错误、保存链路稳定触发、标志位正确回收**。

---

## 3. 连续保存链路（B）逐项验证

`index.saveResult()` 在加固后的 `behaviors/calc-page.js` 中，每轮完整链路实测调用计数如下（期望 = 100，实际 = 100）：

| 链路节点 | 调用次数 |
|----------|----------|
| `wx.createSelectorQuery`（取 Canvas 节点） | 100 |
| `wx.canvasToTempFilePath`（导出图片） | 100 |
| `wx.saveImageToPhotosAlbum`（存相册） | 100 |
| `canvas.createImage`（加载小程序码） | 100 |

- **内存**：连续 100 次保存 + 手动 GC 后，堆 `4.52 MB → 4.67 MB`，**Δ=0.14 MB**，无明显泄漏信号；Canvas 节点与回调每轮正确回收。
- **标志位复位**：末次 `saveResult` 后 `isSaving` 正确复位为 `false`，**无标志位堆积**。

---

## 4. 关键发现

### 4.1 「连点保存」防护是真实健壮性收益
`saveResult` 开头 `if (this.data.isSaving) return;` 在真实异步世界中会把「快速连点第二次」挡掉，避免并发 Canvas 导出 / 存相册打架。压测中 D 维度连点保存 167 次，全部被单例链路安全吸收。

### 4.2 含 Canvas 1.0 饼图也稳
首页饼图 `drawPie` 仍走已弃用的 `wx.createCanvasContext`（S1 技术债，非阻塞）。压测 A / A2 维度每次重算都会触发其同步重绘，5000 次全程稳定、单次均值仅 0.0165 ms——证明它**当前不是性能瓶颈**，但建议后续迁 Canvas 2.0 以消除技术债（见 §6）。

### 4.3 去重重构未引入性能回归
R1（`saveImage` 上提 behavior）、R2（`onInput` 上提）、R3（免责文案统一出口）后，保存链路仅 `calc-page.js` 一处持有，压测结果与重构前一致，无退化。

---

## 5. 诚实边界（真机仍需你验证）

| 本脚本已验证 | 真机 DevTools 需补充验证 |
|--------------|--------------------------|
| 纯 JS 计算正确性、高频不退化 | `setData` 跨桥渲染耗时 |
| 保存链路逻辑稳定、节点回收、内存（Node 堆） | 真机主线程帧率 / 掉帧 / 卡顿 |
| 历史读写稳定性、数组边界 | 真机 `saveImageToPhotosAlbum` 权限弹窗与 I/O |
| 竞态 / 标志位堆积 | 多机型（iOS / 中低端 Android）冷启动表现 |

> 逻辑层已稳，真机渲染与帧率请你在微信开发者工具的 **Audits / 性能面板** 再扫一眼确认。

---

## 6. 建议后续动作（均非阻塞）

1. **S1（技术债）**：将 `pages/index/index.js` 的 `drawPie` 从 `wx.createCanvasContext`（Canvas 1.0）迁到 Canvas 2.0，并视输入项规模对重绘做可选节流。当前不影响功能与上线。
2. **真机复测**：在 DevTools 性能面板跑一遍首页输入风暴 + 连续保存，确认主线程帧率无异常。

---

## 7. 如何复跑

```bash
# 在临时审计目录（与全量测试桩同目录）
cd C:/Users/19625/AppData/Local/Temp/fbc_audit
node --expose-gc stress_test.js
```

- 退出码 `0` = 全部通过；`1` = 存在失败项（脚本会打印失败标签）。
- `--expose-gc` 用于更精确的内存增量采样；不加也能跑，仅内存数字为参考值。
- 脚本自动加载 `miniprogram/` 下真实页面 JS 与 `utils/history.js`，无需改动项目源码。
