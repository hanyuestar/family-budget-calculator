# 开发改动记录

> 项目：聚合计算（family-budget-calculator）
> 最后更新：2026-07-29（财富健康指数上线下，存钱段位已删除）
> 说明：唯一权威改动记录，三合一改造及后续优化均已并入。

---

## 时间线概览

| 日期 | 里程碑 |
|------|--------|
| 2026-07-10 | v3：首页 + 育儿支出 + 保存/分享 + 按钮重构 |
| 2026-07-10 中 | 存钱段位：储蓄诊断 + 财富年龄 + 实物对比 |
| 2026-07-10 晚 | v2.1~v2.2：计算按钮交互、删除参考表、财富年龄重做 |
| 2026-07-14 早 | 测试分析：47/47 计算无bug，产出测试报告 |
| 2026-07-14 中 | 重构：中央注册表/通用报告/通用输入/通用行为 |
| 2026-07-14 晚 | 首页优化：双列网格、自适应列数、删内联Banner |
| 2026-07-14 晚 | 新增 3 工具：BMI、进度条、亲戚关系 |
| 2026-07-14 晚 | Bug修复：首页点击失效（字段名 path≠url） |
| 2026-07-29 | **删除「存钱段位」工具，新增「财富健康指数」替换其首页位**（utils/wealth-health.js + 城市社保/专项扣除数据 + 页面四件套；图标 📈；测试已并入 test_all.js，全量 99/99 + test_tarot 55/55 通过） |
| 2026-07-14 晚 | 亲戚关系算法重写（4层+配偶旁系），对照用户资料二次校正 |
| 2026-07-14 末 | 进度条→时光进度条、亲戚关系归约引擎重构、文档归一化 |
| 2026-07-14 晚 | 三合一改造：暗色主题 + 历史中心 + 存钱段位目标倒推 |
| 2026-07-15 早 | 保存图片模板重构：品牌条 + 2x高清 + hook传播 + 小程序码 |
| 2026-07-15 午 | Canvas 1.0→2.0 全站迁移 + P2-P5 修复 + R1-R4 代码精简 |

---

## 一、页面结构（当前）

### 全局功能
- **暗色主题**：24 个 CSS 变量 + `.is-dark` 覆写层，首页 🌙 一键切换，`storage` 持久化，7 页面 + Canvas（report.js）全覆盖。`getApp().globalData.theme` 单点真源，`calc-page` behavior 自动同步 `isDark`，WXML 根节点 `class="{{isDark ? 'is-dark' : ''}}"` 驱动
- **历史中心**：`utils/history.js` FIFO 100 条 + 收藏 30 上限；历史页 `pages/history/` 含筛选/⭐/删除/清空；首页「📋 历史记录」入口 + 条数徽章
- **历史回填**：点击历史 → `globalData._historyRestore` 传递 → 工具页 `onShow` 自动恢复输入并重新计算
- **写入策略**：仅用户点「保存」按钮时写入历史，不在计算时自动写

### 首页 `pages/home/home`
- 工具选择页，卡片式入口，右上角 🌙 主题切换
- 渐变背景标题区（#667eea → #764ba2）+「📋 历史记录」
- `config/tools.js` 中央注册表驱动，tools 数组加一项即出现卡片
- 自适应网格：≤4工具2列，5~8工具3列，>8工具4列

### 家庭支出计算 `pages/index/index`
- 15项输入：月度固定6项 + 育儿3项 + 月度家庭2项 + 年度4项
- 合计月度/年度总支出 + 保险占比 + 环形饼图（15项）+ 图例
- 保存为图片、分享给好友。`restoreHistory`：15 字段回填

### 财富层级测试 `pages/wealth/wealth`
- 4资产+4负债，单位万元。净资产/负债率 + 7级财富等级表（贫困→巨富）
- 负数回退贫困层。`restoreHistory`：8 字段回填

### 存钱段位 `pages/saving/saving`
- **正向**：月收入+月支出 → 储蓄率 → 5段位（青铜→王者）
- **目标倒推**（新增）：目标金额 + 年限(picker 1-30年) + 月支出 → 每月需存 → 所需月收入 → 段位
- 正向/倒推双模式 toggle，共享段位徽章/财富年龄/实物对比渲染
- 财富年龄（线性插值，12锚点 5%~60%）
- 年度预测 + 实物对比（电动摩托车→二线城市首付）
- 点"计算"按钮才出结果（非实时）。跳转支出计算的引导链接
- `restoreHistory`：自动判断正向/倒推模式（`input.income` vs `input.goalAmount`）

### BMI 计算器 `pages/bmi/bmi`
- 身高cm + 体重kg → BMI值 + 中国标准分类 + 健康体重区间
- `restoreHistory`：身高体重回填

### 亲戚关系 `pages/relation/relation`
- 从「我」出发，选父/母/夫/妻/子/女/兄/弟/姐/妹拼关系链
- 归约引擎：6 条归约规则（姻亲/配偶子女/兄弟姐妹父母/平辈孩子/平辈互消/双层绕路）
- 先归约再查表，超5级链归约后仍超才拒绝
- 五级关系覆盖（祖辈四种兄弟姐妹全量）：爷爷/奶奶/姥爷/姥姥的兄/弟/姐/妹 共 16 条 L3 + 32 条 L4 子女
- 堂/表亲属需选年龄判断长幼
- `restoreHistory`：关系链数组回填 + 计算称呼

### 时光进度条 `pages/progress/progress`
- 零输入：按当前日期自动算今年/本月/今日进度
- 选填出生日期 → 人生进度（预期寿命80岁）
- 进度条可视化 + 保存为图片
- `restoreHistory`：出生日期回填 + 自动算人生进度

---

## 二、公共基础设施

| 模块 | 路径 | 作用 |
|------|------|------|
| 中央工具注册表 | `config/tools.js` | `appName` + `tools` 数组，首页列表唯一数据源 |
| 通用报告生成 | `utils/report.js` | `drawHeader/drawRow/drawDivider/drawBadge/drawFooter/exportAndSave`，Canvas 暗色适配 |
| 通用输入组件 | `components/calc-input/` | 封装 input-row，支持 `symbol`(¥) / `unit`(万元) |
| 通用页面行为 | `behaviors/calc-page.js` | `goHome`(带插屏) + `onShareAppMessage` + 暗色同步 + 历史恢复 |
| 金额格式化 | `utils/format.js` | `formatMoney`(千分位) / `formatWan`(1位小数) |
| 历史存储 | `utils/history.js` | `add/getAll/getByTool/star/remove/clear`，FIFO 100上限 + 收藏30上限 |

---

## 三、加新工具步骤

1. `app.json` 的 `pages` 数组注册页面路径
2. `config/tools.js` 的 `tools` 数组加一项：`{ id, icon, name, desc, path, hot? }`
3. 建 `pages/xxx/xxx.{js,wxml,wxss,json}`：
   - `.js`：`behaviors: [require('../../behaviors/calc-page.js')]`，`data` 含 `share`，输入用 `<calc-input>`，计算完写 `share`，保存调 `report.*`，实现 `restoreHistory`
   - `.json`：`usingComponents: { "calc-input": "/components/calc-input/calc-input" }`
4. （可选）复用全局样式 `.card` / `.btn*` / `.input-row` / `.ad-banner-wrap`

---

## 四、已知坑

### 坑1：首页跳转字段名
- `config/tools.js` 字段名用 **`path`**；`home.wxml` 绑定 `data-path`；`home.js` 读 `dataset.path`
- 三者任一写成 `url`，`wx.navigateTo` 静默失败

### 坑2：亲戚关系
- 引擎：先归约再查表，6条归约规则。归约后仍超5级才拒绝
- 5×2卡片式布局拼链。堂/表亲属需选年龄

### 坑3：按钮通用约定
- `goHome` 来自 `calc-page` behavior，非页面自写
- `saveResult` 依赖 `report.js` canvas 链路，无结果须先 `showToast`
- 分享按钮 `open-type="share"`，无结果应 `disabled`

### 坑4：输入校验
- 各页用 `parseFloat(d.xxx) || 0`，`saving` 页已加 `Math.max(0, …)` 非负校验

### 坑5：命名与环境
- 小程序显示名「聚合计算」→ `config/tools.js` 的 `appName`
- 文件夹名 `family-budget-calculator` 不动

---

## 五、广告位清单

| 位置 | adUnitId | 类型 |
|------|----------|------|
| 首页底部固定 | `adunit-banner-home-002` | Banner |
| 支出计算底部 | `adunit-banner-index-001` | Banner |
| 财富测试底部 | `adunit-banner-wealth-001` | Banner |
| 存钱段位底部 | `adunit-banner-saving-001` | Banner |
| BMI底部 | `adunit-banner-bmi-001` | Banner |
| 时光进度条底部 | `adunit-banner-progress-001` | Banner |
| 亲戚关系底部 | `adunit-banner-relation-001` | Banner |
| 返回首页（插屏） | `adunit-xxxxxxxxxxxxxxxx`（待替换） | 插屏 |

---

## 六、按钮交互规范

- 高度统一 80rpx，圆角 40rpx
- 首页（渐变紫）`.btn-home` / 保存（渐变绿）`.btn-save` / 分享（渐变粉黄）`.btn-share` / 计算（渐变粉红）`.btn-calc` / 重置（灰色）`.btn-secondary`
- 无结果时分享按钮 `disabled`；保存中 `isSaving` 禁用保存按钮

---

## 七、v3 历史改动（2026-07-10）

- 新增首页、育儿支出分组（3项）、输入框改空 placeholder
- 底部按钮重构：返回首页 + 保存结果 + 分享
- canvas 保存结果（支出页/财富页）；返回首页优先 `navigateBack`，否则 `redirectTo`

## 八、存钱段位历史改动（2026-07-10）

- 段位评级（青铜→王者）、财富年龄线性插值（12锚点）、实物对比换算表
- 🔥 火爆角标、财富年龄参考标准 + 数据来源
- 段位参考表（后被删除）、点"计算"才出结果
- Bug：`saving` 双义拆为 `monthSaving` + `isSaving`

## 九、2026-07-14 上午改动

### 1. 进度条 → 时光进度条
- `config/tools.js`：`name: '时光进度条'`；`progress.json`：`navigationBarTitleText: '时光进度条'`

### 2. 亲戚关系归约引擎重构
- 新增 6 条归约规则；先归约再检查长度，支持超长链；修复 L1 递归 bug 和平辈归约缺失
- 5×2 卡片式布局；55/55 测试通过

### 3. 文档归一化
- 合并历史改动到本文件，测试记录到 `测试报告.md`

## 十、2026-07-14 下午修复

### Bug 修复（详见测试报告第八节）
- **P1-1**：`saving.js` income/expense 加 `Math.max(0, …)` 非负校验
- **P1-2**：`wealth.wxss` 淡入动画改为 `@keyframes` + `animation … forwards`
- **P2-1**：`app.js` 插屏 `ad.offClose(done)` 自清理

### 暂缓
- `createCanvasContext` 弃用警告（功能正常，迁移风险高）
- 实时算 vs 点按钮交互差异（产品决策）
- 输入上限校验待定

## 十一、2026-07-14 晚 — 三合一改造

### 1. 暗色主题
- `app.wxss`：24 个 CSS 变量（`--bg-page` / `--bg-card` / `--text-primary` 等）+ `.is-dark` 覆写
- 首页 🌙/☀️ 按钮，`wx.setStorageSync('app_theme')` 持久化
- `report.js` Canvas 通过 `getApp().globalData.theme` 判断色值
- 覆盖 7 页面 wxss + `app.wxss` + `report.js`
- `calc-page` behavior 的 `lifetimes.attached` + `pageLifetimes.show` 读 `globalData` → 设 `isDark`
- 导航栏微信限制无法跟随，固定 `#4A90D9`

### 2. 历史中心
- `utils/history.js`：`add`（unshift 入队，FIFO 100），`getAll`（收藏置顶+倒序），`getByTool`（筛选），`star/remove/clear`
- `pages/history/`：筛选标签（动态生成）+ 列表（icon+摘要+时间 ago）+ ⭐/🗑/清空
- 点击记录 → `app.globalData._historyRestore = item` → `navigateTo`
- 首页「📋 历史记录」+ 条数徽章（`onShow` 刷新）
- `app.json` 注册 `pages/history/history`

### 3. 存钱段位 - 目标倒推
- Toggle 切换（📊 正向 / 🎯 倒推），`mode` 字段 `forward`|`reverse` 驱动互斥 UI
- 倒推输入：目标金额 + 年限(picker 1~30年) + 月支出
- 计算：`goalAmount / months = 月需存 → 月需存 + 月支出 = 所需月收入 → 储蓄率 → 段位`
- 代码重构：`forwardCalc()` + `reverseCalc()`，共享 `matchRank/calcWealthAge/ageComment/calcCompare` 四 helper

### 4. 历史策略调优（深夜）
- **写入时机**：从 `calculate()` 移到 `saveResult()`，仅用户点「保存」时写入
- **历史回填**：`calc-page.js` 的 `pageLifetimes.show` 读取 `_historyRestore` → 调用当前页 `restoreHistory(record)`
  - 支出页：15 字段回填 + calculate
  - 财富页：8 字段回填 + calculate
  - BMI：身高体重回填 + calculate
  - 进度页：出生日期回填 + calcLife
  - 亲戚关系：chain 数组回填 + compute
  - 存钱段位：判断 income/goalAmount → 切对应模式 + 回填 + 计算
- **文案**：底部「数据已自动保存」→「点击『保存』可将结果存入历史」

### 5. 保存图片品牌条（2026-07-15 早）
- **目标**：保存图片自带传播属性——品牌识别 + 小程序码长按跳转 + hook 钩子刺激转发
- **统一模板**：header(渐变标题) → hero(工具主体) → 品牌条(logo+名称+slogan+小程序码+hook) → 极简免责
- **`report.js` 新增** `drawBrandStrip(ctx, { W, bottomY, theme, slogan, hook, qrCode })`：
  - 主题色分隔线 → 浅色圆角卡片（左：logo圆+名称+slogan，右：白底码+长按识别）→ 卡片下 hook 文案
  - 小程序码由 `exportAndSave` 用 `wx.getImageInfo` 精准缩放后叠加
- **2x 高清化**：Canvas 标签 `width/height` 设为逻辑值 2x（如 `style="300px;610px" width="600" height="1220"`），绘图像素翻倍，码清晰可扫
- **Canvas 尺寸统一**：全部提升到 610~670 逻辑高度，品牌条固定 210 逻辑高度
- **6 个工具页全部接品牌条**：
  - 支出：hook「我家一年在『房贷』上花得最多，你呢？」
  - 财富：hook「我的家庭净资产排『钻石家庭』，测测你第几层」
  - 存钱：hook「我的存钱段位是『王者』，财富年龄 35 岁，你呢？」
  - BMI：hook「我的 BMI 是『22.5·正常』，你什么体质？」
  - 进度：hook「2026 已过 53%，你的人生进度到哪了？」
  - 亲戚：hook「我叫 TA『舅公』，来测测你的亲戚关系」
- **去掉旧的分散文案**：`exportAndSave` 不再传 `qrCode`（码在品牌条里画），footer 精简为「数据仅供参考」或空白

### 6. 亲戚关系 LOOKUP 补全
- **缺失**：奶奶的兄弟姐妹（父,母,兄/弟/姐/妹）8 条 L3、姥爷的兄弟姐妹（母,父,兄/弟/姐/妹）8 条 L3，及对应 L4 子女层 16 条
- **补全后**：祖辈四种全部覆盖，`父,母,兄`（奶奶的哥哥）→「舅公/舅爷」✅

## 十二、2026-07-15 午 — Canvas 2.0 + 代码精简 + 品牌条修复

### 1. Canvas 1.0 → 2.0 全站迁移
- `report.js`：`createCanvasContext` → `getContext('2d')`，DPR 自适应 `canvas.width/height = 逻辑×dpr`，所有 `ctx.draw()` 调用删除
- 6 页 WXML：`<canvas type="2d">`、去掉 `width/height` 属性
- `saveResult`：全部改用 `createSelectorQuery().select('#saveCanvas').fields({ node:true, size:true })`
- P2: 小程序码 `qrSize` 84→96，字号 10→11px
- P3: 进度条 `toFixed(0)` → `toFixed(1)`
- P4: `calc-input` 新增 `sanitize` 输入校验
- P5: wealth/bmi/progress 屏上删"仅供参考"，progress `drawFooter` 补免责
- E2: `calc-page` behavior 新增通用 `onInput`，删 index/wealth/bmi 3 页重复代码

### 2. R1-R4 代码精简
- **R1+R4**：`behaviors/calc-page.js` 新增 `saveImage(opts)` 公共脚手架 —— 封装 `createSelectorQuery + drawHeader + drawBrandStrip + drawFooter + exportAndSave`，各页 saveResult 只写自己的 `draw` 回调
  - 6 页 saveResult 全改：index / wealth / bmi / saving / progress / relation
  - 净删 ~150 行重复模板代码
- **R2**：通用 `onInput` 写入 behavior，自动清 `showResult` + 调 `calculate`，删 index/wealth/bmi 3 页重复定义
- **R3**：`saving` → `isSaving` 全局统一（index / wealth 两页用的是旧名），消除变量名歧义
- **R5**：behavior `require('history.js')` 卸载（不需要，仅 `require('report.js')`）
- 全部 16 个 JS 文件 `node --check` 验证通过

### 3. 品牌条布局修复
- **问题1**："长按识别"文字画在卡片边缘外 → 合并到左侧品牌区第三行 `长按识别小程序码 →`
- **问题2**：BMI hook 文案被小程序码挡住 → 新增 `wrapText(ctx,text,maxWidth,maxLines)` 自动换行兜底
- 详见 `bugfix_品牌条布局_20260715.md`
- **缺失**：奶奶的兄弟姐妹（父,母,兄/弟/姐/妹）8 条 L3、姥爷的兄弟姐妹（母,父,兄/弟/姐/妹）8 条 L3，及对应 L4 子女层 16 条
- **补全后**：祖辈四种全部覆盖，`父,母,兄`（奶奶的哥哥）→「舅公/舅爷」✅
